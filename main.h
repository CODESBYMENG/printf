#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stddef.h>

int _putchar(char c);
int _putstr(char *str);
int _putint(int n);
int _putuint(unsigned int n, int base, char *digits);
int _printf(const char *format, ...);


#endif /* MAIN_H */
